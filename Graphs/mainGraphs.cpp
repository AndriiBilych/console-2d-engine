/*Todo: 
* Add comments
* Rewrite calculate position with iterators
* Can you change the size of individual character, not the font?
* Add color to characters "glyphs"
*/

#include "Graph.h"

int main()
{
    Graphs graphs;
    graphs.ConstructConsole(100, 50, 12, 12);

    graphs.StartExecution();

    return 0;
}
